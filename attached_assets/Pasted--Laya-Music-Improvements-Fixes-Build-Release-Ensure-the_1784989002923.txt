# Laya Music – Improvements & Fixes

## Build & Release

* Ensure the project builds successfully on **GitHub Actions** without any errors or warnings.
* Fix all Gradle, dependency, and signing issues that may prevent APK generation.
* Make the project compatible with the latest stable Android SDK and Gradle versions.
* Verify both **Debug** and **Release** builds compile successfully.
* Optimize the CI/CD workflow for faster and more reliable APK generation.

## Lyrics Experience

* Improve **time-synced lyrics** for accurate synchronization with playback.
* Add **automatic lyric scrolling** that smoothly follows the currently playing line.
* Highlight the active lyric line with smooth animations.
* Allow users to **pause, play, skip, and seek** music while the lyrics screen is open.
* Keep lyrics synchronized when seeking through the track.
* Replace the **"ms" timestamp display** with a cleaner interface.
* Add a **"Sync Lyrics"** button to manually re-sync lyrics if they become out of sync.
* Improve lyric loading performance and reliability.
* Cache synced lyrics for offline playback.

## Player Improvements

* Improve playback stability and responsiveness.
* Fix issues with play, pause, next, previous, and seek controls.
* Ensure background playback works consistently.
* Improve notification controls and lock-screen media controls.
* Reduce playback latency and buffering.

## Security

* Remove all **publicly exposed API keys** from the source code.
* Move every API key and secret into **`.env`** or local configuration files.
* Keep the implementation exactly the same while loading values securely from environment variables.
* Ensure no secrets are committed to GitHub.
* Add `.env` to `.gitignore` and provide a `.env.example` template with placeholder values.

## Performance

* Reduce memory usage.
* Improve app startup speed.
* Optimize image loading and caching.
* Improve scrolling performance across the app.
* Reduce unnecessary network requests.
* Optimize battery consumption.

## UI & UX

* Improve animations throughout the app.
* Make transitions smoother and more consistent.
* Fix layout issues on different screen sizes.
* Improve accessibility and touch responsiveness.
* Polish the Material You experience.
* Ensure dynamic colors work correctly across Android versions.

## Downloads

* Improve download reliability.
* Display accurate download progress.
* Fix download resume and retry behavior.
* Ensure downloaded songs are immediately recognized as offline content.

## Playlist

* Fix playlist creation and editing.
* Improve adding and removing songs from playlists.
* Prevent duplicate entries.
* Improve playlist synchronization with YouTube Music.

## Search

* Improve search speed and accuracy.
* Fix search result loading issues.
* Improve suggestions and history.

## General Bug Fixes

* Fix crashes and ANRs.
* Remove deprecated APIs and warnings.
* Improve error handling and logging.
* Resolve memory leaks.
* Clean up unused code and dependencies.
* Improve overall app stability and reliability.
